<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Mini Library — Books</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <main class="container">
      <header class="header">
        <h1>Books</h1>
        <p class="muted">Manage your mini library: add authors, add books, and remove books.</p>
      </header>

      <section class="card">
        <h2>Add an author</h2>
        <form class="form" action="/api/authors" method="POST">
          <label>
            Name
            <input name="name" required placeholder="Barack Obama" />
          </label>
          <button type="submit">Create author</button>
        </form>
        <p class="muted">Tip: create the author first, then pick them when adding a book.</p>
      </section>

      <section class="card">
        <h2>Add a book</h2>
        <form class="form" action="/api/books" method="POST">
          <label>
            Title
            <input name="title" required />
          </label>

          <label>
            Author
            <select name="authorId" required>
              <option value="" selected disabled>Select an author</option>
              <% authors.forEach((a) => { %>
                <option value="<%= a.id %>"><%= a.name %></option>
              <% }) %>
            </select>
          </label>

          <label>
            Genre
            <input name="genre" placeholder="classic, sci-fi, ..." />
          </label>

          <label>
            Year
            <input name="year" type="number" min="0" />
          </label>

          <button type="submit">Create</button>
        </form>

        <p class="muted">
          After you submit, the new book will appear in the list.
        </p>
      </section>

      <section class="card">
        <h2>Current books</h2>
        <p class="muted">This app uses in-memory data (restarting the server resets it).</p>
        <% if (!books.length) { %>
          <p class="muted">No books yet.</p>
        <% } else { %>
          <table class="table">
            <thead>
              <tr>
                <th>Title</th>
                <th>Author</th>
                <th>Genre</th>
                <th>Year</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% books.forEach((b) => { %>
                <tr>
                  <td><%= b.title %></td>
                  <td><%= b.authorName %></td>
                  <td><%= b.genre %></td>
                  <td><%= b.year ?? "" %></td>
                  <td>
                    <div class="actions">
                      <a class="btn" href="/books/<%= b.id %>/edit">Edit</a>
                      <form action="/books/<%= b.id %>/delete" method="POST">
                        <button class="btn btn-danger" type="submit">Delete</button>
                      </form>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        <% } %>
      </section>
    </main>
  </body>
</html>
